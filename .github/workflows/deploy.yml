name: Deploy Portfolio

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Skip GitHub Pages deploy
        run: echo "GitHub Pages deploy is disabled. Production deploys are handled by Vercel."
