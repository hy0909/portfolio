name: Sync to Org

on:
  push:
    branches: [main]

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Push to Org repo
        run: |
          git remote add org https://x-access-token:${{ secrets.ORG_TOKEN }}@github.com/BF-D-challenge/portfolio.git || git remote set-url org https://x-access-token:${{ secrets.ORG_TOKEN }}@github.com/BF-D-challenge/portfolio.git
          git push org HEAD:refs/heads/members/김혜연-portfolio2026 --force
